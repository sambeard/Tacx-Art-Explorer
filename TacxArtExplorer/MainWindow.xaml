<Window x:Class="TacxArtExplorer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TacxArtExplorer" xmlns:components="clr-namespace:TacxArtExplorer.Components"
        xmlns:vm="clr-namespace:TacxArtExplorer.ViewModels"
        xmlns:views="clr-namespace:TacxArtExplorer.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Tacx Art eXplorer" Height="450" Width="800">
    <Window.Resources>
        <!-- Map each ViewModel type to its View -->
        <DataTemplate DataType="{x:Type vm:ArtListViewModel}">
            <views:ArtListView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ArtDetailViewModel}">
            <views:ArtDetailView />
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <!--<Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ListView Grid.Row="1" ItemsSource="{Binding ArtPieces}" Margin="10">
            <components:ArtPieceListItem></components:ArtPieceListItem>
        </ListView>-->
        <ContentControl Content="{Binding CurrentViewModel}" />
    </Grid>
</Window>
